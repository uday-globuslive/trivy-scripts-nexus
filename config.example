# Nexus Vulnerability Scanner Configuration Examples

# Example 1: Basic configuration
# Replace with your actual Nexus server details
NEXUS_URL=https://nexus.example.com
USERNAME=your-username
PASSWORD=your-password
OUTPUT_DIR=./vulnerability_reports

# Example 2: Corporate environment
# NEXUS_URL=https://nexus.corp.company.com:8081
# USERNAME=nexus-scanner
# PASSWORD=SecurePassword123!
# OUTPUT_DIR=C:\VulnerabilityReports

# Example 3: Local development
# NEXUS_URL=http://localhost:8081
# USERNAME=admin
# PASSWORD=admin123
# OUTPUT_DIR=./local_reports

# Security Notes:
# 1. Never commit real passwords to version control
# 2. Use environment variables or secure credential stores in production
# 3. Ensure the scanner account has read-only access to repositories
# 4. Consider using service accounts with limited permissions

# Usage with environment variables:
# On Windows (PowerShell):
# $env:NEXUS_URL="https://nexus.example.com"
# $env:NEXUS_USERNAME="admin"
# $env:NEXUS_PASSWORD="password"
# python nexus_vulnerability_scanner.py --nexus-url $env:NEXUS_URL --username $env:NEXUS_USERNAME --password $env:NEXUS_PASSWORD

# On Linux/macOS (bash):
# export NEXUS_URL="https://nexus.example.com"
# export NEXUS_USERNAME="admin"
# export NEXUS_PASSWORD="password"
# python nexus_vulnerability_scanner.py --nexus-url $NEXUS_URL --username $NEXUS_USERNAME --password $NEXUS_PASSWORD